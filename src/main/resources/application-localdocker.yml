spring:
  cloud:
    discovery:
      enabled: false
  datasource:
    url: jdbc:postgresql://budget-service-db:5432/budgets
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    locations: 'classpath:db/migration'
    baseline-on-migrate: true
#debug: false

---

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/auth/realms/moneymaker
      client:
        registration:
          budget-service:
            provider: keycloak
            client-id: budget-service
            client-secret: qu1hrthelftPL9KRtA66IUZxzGKL33lp
            authorization-grant-type: client_credentials
        provider:
          keycloak:
            token-uri: http://keycloak:8080/auth/realms/moneymaker/protocol/openid-connect/token